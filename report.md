
# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제:
패션 브랜드 앱 UI/UX 디자인 A/B 테스트 만족도 분석 및 마케팅 효율 검증기

이 프로그램을 만든 이유:
패션 마케팅 실무에서는 모바일 앱의 UI/UX 디자인 변경이 브랜드 이미지와 고객 전환율(매출)에 직결되는 매우 중요한 요소입니다. 기존 패션 앱 디자인(A안)과 신규 패션 앱 디자인(B안)에 대한 가상 사용자들의 실제 만족도 데이터를 입력받아, 어떤 디자인이 마케팅 측면에서 더 선호도가 높고 효율적인지 데이터 기반으로 자동 분석하고 의사결정 가이드라인을 제공하기 위해 이 프로그램을 개발했습니다.


---

## 2. 문제 분해 및 모델링

### 입력
- 테스트에 참여할 가상 사용자 수 (정수형 변수: `users_count`)
- 각 가상 사용자별 기존 디자인(A안) 만족도 점수 및 신규 디자인(B안) 만족도 점수 (1~5점 사이의 정수)

### 처리
- 입력받은 데이터를 사용자별로 묶어 하나의 행(1차원 리스트)으로 만들고, 이를 전체 표를 뜻하는 2차원 리스트(`test_data`)에 순차적으로 누적(`append` 활용)
- 반복문을 사용하여 완성된 2차원 리스트를 순회하며 A안과 B안의 총점을 구하고 이를 인원수로 나누어 평균 만족도 계산

### 출력
- 입력된 모든 사용자별 점수 현황 표 (User ID, A안 점수, B안 점수)
- 두 디자인의 최종 평균 점수 비교 문구
- 조건문 판단에 따른 최종 마케팅 의사결정 판정 결과(추천 가이드라인 문구)


---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름: `test_data` (함수 내부 매개변수명: `data_list`)

한 행의 의미: 테스트에 참여한 '가상 사용자 1명'의 전체 평가 데이터 세트

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 가상 사용자 식별 번호 (User ID) | 1 |
| 1번 열 | 기존 패션 앱 디자인(A안) 만족도 점수 | 4 |
| 2번 열 | 신규 패션 앱 디자인(B안) 만족도 점수 | 5 |
| 3번 열 | (해당 없음 - 0~2번 열까지 총 3개 열 사용) | - |

이 2차원 리스트를 사용한 이유:
단일 변수나 일반 1차원 리스트로는 여러 사용자의 독립적인 다중 데이터(유저 ID, A점수, B점수)를 유기적으로 묶어서 관리하기 어렵습니다. 행과 열 구조를 가진 2차원 리스트를 사용함으로써, 실제 엑셀 표처럼 다수의 사용자 데이터를 체계적으로 저장하고, 인덱스 번호(`row[1]`, `row[2]`)를 통해 각 안의 점수만 정확하게 골라내어 계산하기 위해 사용했습니다.


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| `show_intro()` | 프로그램 시작 타이틀 및 안내 출력 | 없음 | 텍스트 출력 |
| `input_scores()` | 사용자 수만큼 만족도 점수를 입력받아 2차원 리스트 생성 | `num_users` (사용자 수) | `data_list` (완성된 2차원 리스트) |
| `calculate_averages()` | 2차원 리스트를 순회하며 두 디자인의 평균 점수 계산 | `data_list` (2차원 리스트) | `avg_A`, `avg_B` (각 안의 평균 점수) |
| `print_report()` | 점수 현황 표 출력 및 최종 마케팅 판정 결과 출력 | `data_list`, `avg_A`, `avg_B` | 표 형태의 데이터 및 마케팅 추천 문구 출력 |
| `main()` | 전체 함수들의 실행 흐름과 데이터를 제어하는 총감독 역할 | 없음 | 프로그램 전체 가동 및 제어 |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: `input_scores(num_users)`

설명:
이 함수는 사용자가 입력한 참여자 수(`num_users`)만큼 `for` 반복문을 돌며 각 사용자의 패션 앱 만족도 점수를 `input()`으로 입력받습니다. 입력받은 문자열 데이터를 연산이 가능하도록 `int()`를 통해 정수형 숫자로 변환한 뒤, `[i, a_score, b_score]` 형태의 1차원 리스트(행)를 만듭니다. 최종적으로 이 행을 큰 리스트에 `.append()` 명령어로 추가하여 행과 열 구조를 가진 2차원 리스트를 동적으로 구축하고 반환하는 프로그램의 핵심 입력 모듈입니다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분
`print_report` 함수 내부에서 A안과 B안의 최종 평균 만족도 점수를 비교할 때 사용했습니다.
어떤 조건을 검사했는가?
- `if avg_B > avg_A`: 신규 디자인(B안)의 평균 만족도가 더 높은지 검사하고, 점수 차이가 1.0점 이상으로 압도적인지 (`if (avg_B - avg_A) >= 1.0`) 중첩 조건문으로 세부 검사합니다.
- `elif avg_A > avg_B`: 기존 디자인(A안)의 선호도가 더 높은지 검사합니다.
- `else`: 두 디자인의 선호도가 완벽히 동일한지 검사합니다.

### 반복문 사용 부분
무엇을 반복 처리했는가?
- `input_scores` 함수에서 지정된 가상 사용자 수만큼 만족도 점수 입력을 반복 처리했습니다.
- `calculate_averages` 함수에서 2차원 리스트의 행을 하나씩 꺼내며 각 디자인의 점수(`row[1]`, `row[2]`)를 총점 박스에 누적하여 더하는 처리를 했습니다.
- `print_report` 함수에서 2차원 리스트에 누적되어 저장된 모든 유저의 데이터를 표 형태로 줄 바꿈하며 순차 출력했습니다.

### 조건문과 반복문이 함께 작동한 부분
`print_report` 함수에서 `for row in data_list:` 반복문을 통해 2차원 리스트 안의 모든 사용자 데이터를 화면에 한 줄씩 표 형태로 출력하는 처리를 먼저 완료한 뒤, 바로 아래에서 계산된 평균값을 바탕으로 `if-elif-else` 조건문을 실행하여 어떤 마케팅 제안(즉시 반영, 보류 등)을 할지 최종 판정하여 함께 출력하도록 구현했습니다.


---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text
==================================================
   패션 브랜드 앱 UI/UX 디자인 A/B 테스트 분석기
   기존 디자인(A) vs 신규 디자인(B) 선호도 분석
==================================================
테스트에 참여할 사용자 수는 몇 명인가요?: 3

--- 총 3명의 UI/UX 테스트 데이터를 입력받습니다 ---

[1번 가상 사용자 평가]
기존 디자인(A안) 만족도 점수 (1~5점): 4
신규 디자인(B안) 만족도 점수 (1~5점): 5

[2번 가상 사용자 평가]
기존 디자인(A안) 만족도 점수 (1~5점): 3
신규 디자인(B안) 만족도 점수 (1~5점): 5

[3번 가상 사용자 평가]
기존 디자인(A안) 만족도 점수 (1~5점): 4
신규 디자인(B안) 만족도 점수 (1~5점): 4

=============== A/B TEST REPORT ===============
사용자ID	A안 점수	B안 점수
---------------------------------------------
User 1	4점		5점
User 2	3점		5점
User 3	4점		4점
---------------------------------------------
■ 최종 평균 만족도 -> A안: 3.67점 / B안: 4.67점
===============================================

[마케팅 의사결정 판정 결과]
-> 결과: 신규 패션 앱 디자인(B안)의 만족도가 더 높습니다.
💡 [추천] 신규 디자인 즉시 반영 및 전면 교체를 권장합니다!

```

결과 분석:
사용자 3명의 데이터를 입력받아 분석한 결과, 기존 디자인(A안)의 최종 평균 점수는 3.67점인 반면 신규 패션 앱 디자인(B안)의 평균 점수는 4.67점으로 계산되었습니다. 신규 디자인(B안)이 기존 디자인보다 정확히 1.0점 더 높게 나왔기 때문에, 시스템은 중첩 조건문 판단에 따라 신규 디자인을 패션 앱 서비스에 즉시 반영하고 전면 교체하라는 마케팅 의사결정 추천 문구를 정확하게 출력했습니다.

---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력: 사용자 수 2명 / 1번 유저(A: 3, B: 4) / 2번 유저(A: 2, B: 5)
예상 결과: A안 평균 2.50점, B안 평균 4.50점 -> B안 우세 및 즉시 반영 추천 문구 출력
실제 결과: 예상했던 평균값 계산 및 표와 마케팅 판정 추천 문구가 완벽히 일치하게 출력됨.

### 테스트 2: 예외 또는 경계 사례

입력: 사용자 수 2명 / 1번 유저(A: 3, B: 3) / 2번 유저(A: 4, B: 4)
예상 결과: A안 평균 3.50점, B안 평균 3.50점 -> 두 안의 만족도가 완벽히 동일하다는 예외 판정 출력
실제 결과: `else` 구문이 정상 작동하여 "두 디자인의 만족도가 완벽히 동일합니다. 추가 데이터 수집 및 디자인 재테스트를 권장합니다" 문구가 올바르게 출력됨.

### 발견한 오류와 수정한 방법

* **발견한 오류:** 초기 코드 설계 시 프로그램 최상단에 전역 변수 형태로 `test_data = []`를 비워둔 채 선언했으나, 정작 데이터를 처리하는 `main()` 함수 내부에서는 `my_data = input_scores(users_count)`라는 별개의 변수명을 사용하여 최상단 변수가 활용되지 않고 메모리만 차지하는 '죽은 변수' 비효율성이 존재했습니다.
* **수정한 방법:** 동료 피드백 과정을 통해 해당 비효율성을 명확히 인지하였고, 가독성을 높이기 위해 불필요한 최상단 변수 선언을 삭제했습니다. 대신 `main()` 함수 내부에서 데이터를 제어하는 핵심 변수 이름을 `test_data`로 일관성 있게 수정 및 매핑하여 전체적인 프로그램의 데이터 흐름 구조를 간결하고 직관적으로 리팩토링했습니다.

---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:
전체적인 프로그램 설계 구조(입력-처리-출력의 함수 분리)와 2차원 리스트 활용 뼈대를 구축하는 데 도움을 받았습니다. 특히 파이썬의 기초 수업 범위를 넘어 실무 프로그래밍에서 출력 가독성을 높여주는 `f-string` 포맷팅 기법, 함수의 역할을 주석으로 명시하여 구조를 설명하는 `Docstring(독스트링)` 문법, 문자열 곱하기(`*`)를 활용한 가로 구분선 인테리어 등의 심화 표현법을 배우고 코드에 적용할 수 있었습니다.

Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:
Gemini는 최초에 특정 진로 분야가 연계되지 않은 일반적이고 광범위한 문구의 'UI/UX 디자인 A/B 테스트 분석기' 기본 템플릿 코드를 제안했습니다. 하지만 저는 제 관심 진로 분야인 '패션 마케팅'적 요소를 수행평가에 녹여내고 싶었기 때문에, 제미나이가 제안한 기본 타이틀과 출력 문구들을 '패션 브랜드 앱 디자인 만족도' 및 '패션 마케팅 의사결정 판정 결과'라는 맞춤형 텍스트로 직접 수정하여 반영했습니다.

최종 코드에서 내가 직접 판단하거나 수정한 부분:
동료 피드백 과정을 거치며 코드 내부에서 변수명이 통일되지 않아 흐름을 직관적으로 파악하기 어렵게 만들던 `test_data`와 `my_data` 변수의 불일치 문제를 스스로 판단하여 하나로 통합·수정했습니다. 또한, 마케팅 효율 검증이라는 프로그램의 목적에 걸맞게 두 디자인의 점수 차이가 1.0점 이상 벌어졌을 때 출력되는 강력 추천 가이드라인 문구를 중첩 if문 형태로 직접 설계하여 추가했습니다.

---

## 9. 한계와 개선 방안

현재 프로그램의 한계:
현재 프로그램은 사용자가 숫자가 아닌 문자(예: '오점' 또는 '안함')를 입력하거나, 점수 범위를 완전히 벗어난 숫자(예: 0점 또는 100점)를 입력했을 때 시스템이 에러(ValueError)를 일으키며 멈추거나 엉뚱한 평균값을 계산하여 전체 마케팅 통계 데이터를 오염시키는 예외 처리 한계가 있습니다.

다시 개선한다면 추가하고 싶은 기능:
사용자가 잘못된 형식이나 범위를 벗어난 만족도 점수를 입력했을 때 프로그램이 튕기지 않도록, `while` 반복문과 `try-except` 예외 처리 문법을 도입하여 "1~5점 사이의 정수만 다시 입력해 주세요"라고 경고를 띄우고 올바른 값이 들어올 때까지 지속해서 입력을 유도하는 '데이터 유효성 필터링 및 재입력 시스템 기능'을 추가하고 싶습니다.



## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?

---
